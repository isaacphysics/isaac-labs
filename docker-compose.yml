version: '2'
services:
  blog:
    container_name: labs-blog
    image: jekyll/jekyll:3.1.2
    volumes:
      - ./blog:/srv/jekyll:rw
    command: sudo jekyll server
    restart: always
    networks:
      default:
        aliases:
          - labs
networks:
  default:
    external:
      name: isaac
